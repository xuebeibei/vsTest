<HISGUICore:HISGUIViewBase x:Class="HISGUIPatientCardLib.Views.PatientCardManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HISGUIPatientCardLib.Views"
             xmlns:HISGUICore="clr-namespace:HISGUICore;assembly=HISGUICore"
             xmlns:Controls="clr-namespace:HISGUICore.MyContorls;assembly=HISGUICore">
    <HISGUICore:HISGUIViewBase.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HISGUIPatientCardLib;component/MyDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </HISGUICore:HISGUIViewBase.Resources>
    <Grid Background="#EEEEEE">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Background="#38434E">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <Label Foreground="White">Logo</Label>
                <Label Foreground="White">就诊卡管理</Label>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <TextBlock x:Name="Tt" Margin="5" TextWrapping="Wrap"></TextBlock>
                <Label x:Name="UserName" Foreground="White" Content="{Binding CurrentUser.Username}"></Label>
                <Button Click="LayoutBtn_Click" Style="{DynamicResource MyToolBtn}">退出</Button> 
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Margin="5" Grid.Row="0" Grid.Column="0" Click="Button_Click">办卡</Button>
                <Button Margin="5" Grid.Row="0" Grid.Column="1">充值</Button>
                <Button Margin="5" Grid.Row="1" Grid.Column="0">退卡</Button>
                <Button Margin="5" Grid.Row="1" Grid.Column="1">挂失</Button>
            </StackPanel>
            <WebBrowser Name="Web" Margin="5" Grid.Column="0" Grid.Row="1">
            </WebBrowser>
        </Grid>
    </Grid>
</HISGUICore:HISGUIViewBase>
