<HISGUICore:HISGUIViewBase x:Class="HISGUIClinicDoctorLib.Views.ClinicRecipe"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HISGUIClinicDoctorLib.Views"
             xmlns:HISGUICore="clr-namespace:HISGUICore;assembly=HISGUICore"
             xmlns:Controls="clr-namespace:HISGUICore.MyContorls;assembly=HISGUICore">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>过敏史：</Label>
                <TextBlock></TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SelectTempletBtn" Margin="5" Click="SelectTempletBtn_Click">选择模板</Button>
                <Button x:Name="SelectDrugBtn" Margin="5" Click="SelectDrugBtn_Click">选择药品</Button>
                <Button x:Name="CopyRecipeBtn" Margin="5" Click="CopyRecipeBtn_Click">复制处方</Button>
            </StackPanel>
            <TabControl x:Name="tabControl"  Margin="5" VerticalAlignment="Top"  Grid.Row="1" Grid.Column="1">
                <TabItem Header="西/成药处方">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!--<DataGrid x:Name="xiyao" Margin="5" ItemsSource="{Binding}" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="组号"
                                            Width="40"
                                            Binding="{Binding Name}" />
                                    <DataGridTextColumn Header="药品名"
                                            Width="90"
                                            Binding="{Binding Unit}" />
                                    <DataGridTextColumn Header="单次计量"
                                            Width="80"
                                            Binding="{Binding Value}" />
                                    <DataGridTextColumn Header="用法"
                                            Width="80"
                                            Binding="{Binding LowerLimit}" />
                                    <DataGridTextColumn Header="皮试"
                                            Width="40"
                                            Binding="{Binding LowerLimit}" />
                                    <DataGridTextColumn Header="用药频次"
                                            Width="80"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="天数"
                                            Width="40"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="总量"
                                            Width="40"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="说明"
                                            Width="80"
                                            Binding="{Binding UpperLimit}" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button Margin="5" Width="20" Click="Button_Click_1">+</Button>
                                <Button Margin="5" Width="20" Click="Button_Click_2">-</Button>
                                <Button Margin="5" Click="Button_Click_3">保存</Button>
                                <Button Margin="5" Click="Button_Click_4">保存模板</Button>
                                <Button Margin="5" Click="Button_Click_5">打印处方</Button>
                            </StackPanel>-->
                            <Controls:MyTableEdit></Controls:MyTableEdit>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="中药处方">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!--<DataGrid x:Name="zhongyao" Margin="5" ItemsSource="{Binding}" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="组号"
                                            Width="40"
                                            Binding="{Binding Name}" />
                                    <DataGridTextColumn Header="药品名"
                                            Width="90"
                                            Binding="{Binding Unit}" />
                                    <DataGridTextColumn Header="单次计量"
                                            Width="80"
                                            Binding="{Binding Value}" />
                                    <DataGridTextColumn Header="用法"
                                            Width="80"
                                            Binding="{Binding LowerLimit}" />
                                    <DataGridTextColumn Header="用药频次"
                                            Width="80"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="天数"
                                            Width="40"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="总量"
                                            Width="40"
                                            Binding="{Binding UpperLimit}" />
                                    <DataGridTextColumn Header="说明"
                                            Width="80"
                                            Binding="{Binding UpperLimit}" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button Margin="5" Width="20" Click="Button_Click_6">+</Button>
                                <Button Margin="5" Width="20" Click="Button_Click_7">-</Button>
                                <Button Margin="5" Click="Button_Click_8">保存</Button>
                                <Button Margin="5" Click="Button_Click_9">保存模板</Button>
                                <Button Margin="5" Click="Button_Click_10">打印处方</Button>
                            </StackPanel>-->
                            <Controls:MyTableEdit></Controls:MyTableEdit>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </StackPanel>
    </Grid>
</HISGUICore:HISGUIViewBase>
