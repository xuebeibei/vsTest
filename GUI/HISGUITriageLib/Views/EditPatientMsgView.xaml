<HISGUICore:HISGUIViewBase  x:Class="HISGUINurseLib.Views.EditPatientMsgView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HISGUINurseLib.Views"
             xmlns:HISGUICore="clr-namespace:HISGUICore;assembly=HISGUICore"
             xmlns:Controls="clr-namespace:HISGUICore.MyContorls;assembly=HISGUICore">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Controls:PersonalInformation></Controls:PersonalInformation>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="确认" Command="{Binding EditPatientMsgOKCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>
            <Button Content="取消" Command="{Binding EditPatientMsgCancelCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>

        </StackPanel>
    </Grid>
</HISGUICore:HISGUIViewBase>
