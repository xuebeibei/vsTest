<HISGUICore:HISGUIViewBase  x:Class="HISGUITriageLib.Views.SelectDoctorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HISGUITriageLib.Views"
             xmlns:HISGUICore="clr-namespace:HISGUICore;assembly=HISGUICore"
             xmlns:Controls="clr-namespace:HISGUICore.MyContorls;assembly=HISGUICore">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="确认" Click="Button_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>
            <Button Content="取消" Command="{Binding SelectDoctorCancelCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>
        </StackPanel>
        <Controls:DoctorFind x:Name="DoctorFindList" Grid.Row="0"></Controls:DoctorFind>
    </Grid>
</HISGUICore:HISGUIViewBase>
