<UserControl x:Class="HISGUITriageLib.Views.DoctorFind"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HISGUITriageLib.Views"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBox Width="50" Margin="0,5,0,5"></TextBox>
            <Button Margin="0,5,5,5">查找</Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="5"></DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Margin="5">确定</Button>
            <Button Margin="5,5,0,5">取消</Button>
        </StackPanel>
    </Grid>
</UserControl>
